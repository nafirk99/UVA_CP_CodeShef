using System;
					
public class Program
{
	public static void Main()
	{
		string name = Console.ReadLine();
		char[] charX = name.ToCharArray();
		Array.Sort(charX);
		name = new string (charX);
		
		int len = name.Length;
		int count = len;
		for(int i=0, j=0; i<name.Length-1; i++, j++)
		{
			if ((name[i] == name[j+1]) )
			{
				count--;
			}
		}
		//Console.WriteLine(count);
		if(count%2 != 0 )
		{
			Console.WriteLine("IGNORE HIM!");
		}
		else Console.WriteLine("CHAT WITH HER!");
	}
}